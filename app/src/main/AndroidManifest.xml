<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="nredondo26.com.fastjob">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ConfirmarActivity"></activity>
        <activity android:name=".Fin_registro_Activity" />
        <activity android:name=".Escoger_perfil_Activity" />
        <activity android:name=".Registro_Candidato_Activity" />
        <activity android:name=".Registro_Empresa_Activity" />
        <activity
            android:name=".SeleccionadosActivity"
            android:label="@string/title_activity_seleccionados"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AnunciaActivity"
            android:label="@string/title_activity_anuncia"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>